# ----------------- Driver ----------------------
class Driver:
""" Represents a driver agent that can move on the map and accept or reject offers
 Attributes (minimum):
 - id: int
 - position: Point
 – speed: float (units per tick)
 – status: str (for example"IDLE","TO_PICKUP","TO_DROPOFF")
 – current_request: Request | None
 – behaviour: DriverBehaviour
 – history: list (to store recent completed trips, earnings, and similar statistics)"""
    # Methods minimum:
    def assign_request(request: Request, current_time: int)-> None:
        #mangler

    def target_point()-> Point | None:
        #mangler:  Returns the next target: the pickup point if status is"TO_PICKUP", or the drop-off point if status is"TO_DROPOFF".

    def step(dt: float)-> None:
        #mangler:  Moves the driver towards the current target according to speed and the time step dt.

    def complete_pickup(time: int)-> None:
        #mangler:  Updates internal state when the pickup is reached.

    def complete_dropoff(time: int)-> None:
        #mangler: Updates internal state and history when the drop-off is reached.
